import React from 'react'
import LoadingClient from '../_components/LoadingClient'

const loading = () => {
  return (
    <LoadingClient width='w-screen' height='h-screen'/>
  )
}

export default loading